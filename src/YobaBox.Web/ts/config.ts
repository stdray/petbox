// Config page interactivity — expand/collapse, inline edit toggle, tag tokenizer.
// Loaded lazily on the /config page via Alpine.js.

document.addEventListener("alpine:init", () => {
	// Config-specific components will be registered here.
});

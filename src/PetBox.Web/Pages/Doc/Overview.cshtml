@page "/doc/overview"
@model PetBox.Web.Pages.Doc.OverviewModel
@{
	ViewData["Title"] = "PetBox overview — docs";
	Layout = "_PublicLayout";
}
	@* Thin shell: the prose is the markdown canon Pages/Doc/content/overview.md, rendered on the
	   server through the shared IMarkdownRenderer (via _MdBody). No HTML duplication to hand-sync. *@
	<main class="max-w-3xl mx-auto p-6" data-testid="doc-overview">
		<p class="text-sm opacity-60"><a href="@Routes.Doc()">← docs</a></p>
		<article>
			<partial name="_MdBody" model="@(new PetBox.Web.Pages.Shared.MdBodyModel { Body = Model.Markdown, TestId = "doc-overview-body" })" />
		</article>
	</main>

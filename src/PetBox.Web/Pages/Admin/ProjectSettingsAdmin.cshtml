@page "/ui/admin/ws/{workspaceKey}/projects/{projectKey}/settings"
@model PetBox.Web.Pages.Admin.ProjectSettingsAdminModel
@using PetBox.Core.Settings
@using PetBox.Web.Settings
@{
	ViewData["Title"] = "Project settings";
	Layout = "_AdminLayout";
}

<div class="mb-4">
	<h1 class="text-2xl font-semibold" data-testid="proj-settings-title">@Model.ProjectKey project settings</h1>
	<p class="text-sm opacity-60">Settings with TopLevel = Project. Owned here; higher scopes (workspace/system) fall back to their own Defaults pages when this project hasn't set its own value.</p>
</div>

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
	<div class="alert alert-success mb-3 max-w-3xl" data-testid="proj-settings-success">@Model.SuccessMessage</div>
}
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
	<div class="alert alert-error mb-3 max-w-3xl" data-testid="proj-settings-error">@Model.ErrorMessage</div>
}

@if (Model.Sections.Count == 0)
{
	<div class="text-xs opacity-60 italic" data-testid="proj-settings-empty">No settings configurable at project scope.</div>
}
else
{
	@foreach (var grp in Model.Sections)
	{
		var groupName = grp.RecordType.Name;
		var fieldsModel = new SettingsFormFieldsModel
		{
			RecordType = grp.RecordType,
			Current = grp.Current,
			CurrentScope = Scope.Project,
		};
		<h2 class="text-lg font-semibold mt-6 mb-2" data-testid="@($"proj-settings-section-{groupName}")">@groupName</h2>
		<form method="post" asp-page-handler="Save" class="card bg-base-200 border border-base-300 max-w-3xl mb-3" data-testid="@($"proj-settings-form-{groupName}")">
			@Html.AntiForgeryToken()
			<input type="hidden" name="recordType" value="@groupName" />
			<div class="card-body p-4">
				<partial name="_SettingsFormFields" model="fieldsModel" />
				<div class="card-actions mt-3">
					<button type="submit" class="btn btn-sm btn-primary" data-testid="@($"proj-settings-save-{groupName}")">Save</button>
				</div>
			</div>
		</form>
	}
}

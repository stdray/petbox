@page "/ui/admin/ws/{workspaceKey}/projects/{projectKey}/log"
@model PetBox.Web.Pages.Admin.ProjectLogSettingsModel
@{
	// OnGet redirects to the project Info page (/info), which owns log retention. This body
	// never renders — it exists only so the /log route stays valid for old links/bookmarks.
	Layout = null;
}

{"type":"step_start","timestamp":1788801686061,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce39a2b001Z9vST69BEqfKP9","messageID":"msg_07ce39242001stAyAOFxW2izj8","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801688566,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"skill","callID":"call_00_YTjiWhM6dW9WxLTtd4q39028","state":{"status":"completed","input":{"name":"petbox"},"output":"<skill_content name=\"petbox\">\n# Skill: petbox\n\nThis project is connected to a PetBox instance over MCP (server `petbox`, https://petbox.3po.su).\r\nPass projectKey \"probe-m1-sandbox\" in every call (the key is scoped to the probe-m1-sandbox project;\r\nboards/memory/sessions live at https://petbox.3po.su/ui/stdray/probe-m1-sandbox).\r\n\r\n**Tool naming:** the base verbs are underscore-delimited (`tasks_upsert`, `memory_search`, …).\r\nIn opencode the MCP tools are `petbox_<verb>` (e.g. `petbox_tasks_upsert`, `petbox_memory_search`);\r\nin Claude Code they are `mcp__petbox__<verb>`. Just prefix the base verb per runtime.\r\n\r\n**Task nodes are FLAT slugs** (`key` = [a-z][a-z0-9_-]*); hierarchy is the `partOf` edge,\r\ngrouping is `tags` (`area:*` / `concern:*`). Give each node a short `title` and a markdown\r\n`body`. A cold `tasks_upsert` to an **unknown** board is rejected (with a did-you-mean) —\r\ncreate it first with `tasks_board_create` (or a methodology). The upsert response is a pure ack for\r\nYOUR call (added/updated/removed cover only your nodes); to catch up on everyone's changes\r\ncall `tasks_delta` with `sinceVersion` = a previous `currentVersion`. `nodes`/`entries` are\r\nTYPED arrays — pass real JSON arrays, not stringified JSON.\r\n\r\n**`tasks_search` is THE read verb** — two modes: without `q` it's a deterministic LISTING\r\n(pass `board` for one board, omit for the whole project; default order priority-then-key),\r\nwith `q` it's hybrid relevance search (FTS ⊕ vectors). Filters work in both modes:\r\n`status[]`, `nodes[]`, `underNode` (subtree), `statusKind[]` (visibility: `open` |\r\n`terminalok` | `terminalcancel` — omit for the mode default, name all three to see everything;\r\nthere is no `includeClosed`); `sort{by,desc}`\r\nreorders; `bodyLen` snippets bodies. One node in full: `tasks_node_get`.\r\nEvery **node reference** (`nodes[]`, `underNode`, `node`, `partOf`, `blockedBy`,\r\n`supersedes`, and relations' `from`/`to` in BOTH the single and the batch form) takes a slug\r\nkey **or** a 32-hex NodeId — both accepted. The NAME tells you: a node reference never ends in\r\n`Id`/`Key`/`Slug`, exactly because it accepts either spelling (there is no `fromNodeId`\r\nany more). A type suffix means it is NOT a reference: `tasks_upsert`'s `key` is the slug FIELD\r\nbeing written — slug only, and REQUIRED on every node — `parentId` is a comment id, and\r\nrelations' `id`/`ids` are edge ids. `NodeId`-suffixed names appear only in RESPONSES, where the\r\nvalue really is always a NodeId.\r\n\r\n**Memory entries are typed** (`User` | `Feedback` | `Project` | `Reference`) — `type` is\r\nrequired only when `memory_upsert` creates a NEW entry (version 0), not on an edit;\r\n`tags` is an ARRAY of strings ([] clears, omit keeps).\r\n`memory_search` is THE read verb: with `q`\r\na hybrid relevance search (FTS ⊕ vectors), without `q` a deterministic listing (updated\r\ndesc); no `scope` cascades project ⊕ workspace over every store (use `bodyLen` for snippets).\r\n\r\n**Canon** — SessionStart injects an index from memory store `canon`, key `index` (per\r\nscope: `probe-m1-sandbox` project / `stdray`). To edit it: `memory_upsert` with\r\n`store:\"canon\"`, `entries:[{key:\"index\", version:<from your last read>, body:\"...\"}]` at\r\nthe matching scope — the key already exists, so `version:0` always conflicts; read first\r\n(`memory_get`/`memory_search`). Keep it a compact index of pointers, not a growing doc.\r\nPetBox memory is primary — your harness's own local notes/autoindex is\r\nsecondary, never a parallel store. Propose promoting a fact to canon when it keeps getting\r\nre-derived, a rule keeps getting repeated, or a gotcha bites twice — the owner curates, you\r\ndon't push silently.\r\n\r\n**What goes where:**\r\n- Session (`session_*`) — the current working plan/thinking. \"Stale next week?\" → session.\r\n- Tasks (`tasks_*`) — a unit of work with a status tracked to Done.\r\n- Memory (`memory_*`) — a durable fact that should outlive the work. Don't store what\r\n  code/git already records, transient state, secrets, or actionable work (that's a task).\r\n- Observations (board `observations`) — a defect-like finding: something broken, behaving\r\n  unexpectedly, or contradicting docs. NOT a memory fact and not (yet) a task.\r\n\r\n**Observations** — every project has a built-in, undeletable `observations` board (world\r\n`$utility`, so it never enters the owner's decision queue or digest). It's an ordinary\r\nboard (`tasks_search`/`tasks_node_get`/`tasks_upsert` work on it like any other) holding\r\n`observation` nodes: `seen` → `promoted` → `fixed` (terminal ok), or `declined` (terminal\r\ncancel) — a status value, no separate FSM engine. Writing a similar node there dedupes onto\r\nthe existing one and bumps its `recurrenceCount` instead of creating a duplicate (surfaced as\r\n`deduped` in the write response and as `observation.recurrenceCount`/`lastSeenAt` on search\r\nhits) — this only fires on a purely-creating batch (every node `version:0`, no deletes), so\r\ndon't mix creates with edits in one call. `tasks_observation_promote` turns a `seen`\r\nobservation into a real `work` (needs `type: feature|bug|chore`) or `ideas` node via an\r\n`observation_obligation` relation; the observation moves to `promoted` and stays addressable,\r\nit does not disappear. When the linked task/idea reaches a terminal-ok status the observation\r\nauto-flips to `fixed`; a terminal-not-ok reopens it to `seen`. A fresh hit of the same problem\r\nafter a fix reopens it to `seen` too, stamps `recurredAfterFixAt`, and flags\r\n`decisionPending:true` on the task that had \"fixed\" it — that task's terminal status is not\r\ntouched automatically. The session extractor also files defect-like findings here on its own;\r\nyou don't need to do that manually for anything it would already catch.\r\n\r\n**Tools:**\r\n- `tasks_board_list / board_create / board_delete / search / node_get / upsert / delta / workflow`\r\n- `tasks_observation_promote` — promote a `seen` observation to `work`/`ideas` (see above)\r\n- `memory_store_list / store_create / store_delete / search / remember / get / upsert / delta`\r\n- `session_search / get / upsert / append / delete` (`search` without `q` = the session listing; with `q` = two-stage archive search whose hits carry message ordinals for `session_get`)\r\n- Logs: `log_query` (KQL), `log_create / list / delete`\r\n- Admin (per-type, flat params): `project_create / list`, `apikey_create / list / delete`,\r\n  `db_create / list / delete / describe`\n\nBase directory for this skill: C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox\nRelative paths in this skill (e.g., scripts/, reference/) are relative to this base directory.\nNote: file list is sampled.\n\n<skill_files>\n\n</skill_files>\n</skill_content>","metadata":{"name":"petbox","dir":"C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox","truncated":false},"title":"Loaded skill: petbox","time":{"start":1788801688536,"end":1788801688559}},"id":"prt_07ce3a356001JDpDiptqjz5lcV","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce39242001stAyAOFxW2izj8"}}
{"type":"tool_use","timestamp":1788801688747,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"skill","callID":"call_01_7N2BM5yfN1bghrCi6C132681","state":{"status":"completed","input":{"name":"petbox-node-authoring"},"output":"<skill_content name=\"petbox-node-authoring\">\n# Skill: petbox-node-authoring\n\n# Body authoring: <REDACTED> for a reader, not for yourself\r\n\r\n## (a) What the renderer already gives you, for free\r\n\r\nA node/comment body is rendered as GFM: `##` headings, real paragraph breaks (a blank line — not\r\na literal `\\n`, not `==heading==`), bullet and numbered lists, tables, fenced code blocks, links.\r\nTypography, light/dark theming, line-length measure, and telling prose apart from identifiers are\r\nall the renderer's job — you do not pay for any of it and should not invent your own ASCII\r\nsubstitute (box-drawing dividers, hand-aligned columns, `**bold**` used as a heading). Write plain\r\nGFM; the renderer does the rest. This skill is FORMAT only — if the body is long or non-ASCII text\r\nyou are composing right now, its DELIVERY (write it to a file, then pass `bodyRef` instead of\r\ninlining) is `petbox-write-economy`: open that skill before the write call in that case, not this\r\nchecklist alone.\r\n\r\n## (b) Callout: GFM alerts, not a fake blockquote\r\n\r\nA statement that must read as something OTHER than surrounding prose — a warning, a decision, a\r\ncaveat the reader must not skim past — is a GFM alert:\r\n\r\n```\r\n> [!NOTE]\r\n> Point worth flagging but not urgent.\r\n\r\n> [!WARNING]\r\n> This will break X if skipped.\r\n```\r\n\r\nThe five kinds are `NOTE`, `TIP`, `IMPORTANT`, `WARNING`, `CAUTION` — each renders with its own\r\nlabel and styling; a plain `>` blockquote with no `[!KIND]` line is just a quote, not a callout.\r\nDo not simulate one with bold text or a horizontal rule; the renderer only recognizes this exact\r\nform.\r\n\r\n## (c) Diagram: a sanitized inline-SVG subset, not mermaid\r\n\r\nA body may contain raw `<svg>...</svg>` markup, sanitized at render time against a pinned tag/\r\nattribute allowlist (`svg g path rect circle ellipse line polyline polygon text tspan marker defs\r\ntitle desc use`, plus geometry/presentation/marker attributes). There is no mermaid fence and no\r\nother diagram DSL — mermaid was considered and rejected for this feature. `<style>`, `<script>`,\r\n`<foreignObject>` and `<image>` are stripped outright; an external `href`/`url(...)` reference is\r\nrejected and only a local `#id` reference survives. Color it with `fill=\"currentColor\"` /\r\n`stroke=\"currentColor\"` — that alone makes the diagram theme correctly in light and dark for free;\r\ndo not hardcode hex colors.\r\n\r\nEvery diagram is a `<figure>`, and both halves must carry the SAME claim:\r\n\r\n```\r\n<figure>\r\n<svg viewBox=\"0 0 200 100\" role=\"img\">\r\n<title>The bridge is struck through; the dashed line addresses a different id space.</title>\r\n... shapes ...\r\n</svg>\r\n<figcaption>The bridge is struck through; the dashed line addresses a different id space.</figcaption>\r\n</figure>\r\n```\r\n\r\n- `role=\"img\"` + `<title>` is the drawing's own text alternative — write it as a full sentence\r\n  stating what the picture shows, not a label like \"Diagram 1\".\r\n- The `<figcaption>` states the same claim in prose, for a reader who does not parse the SVG.\r\n- If you cannot write that one sentence, the diagram does not have a clear point yet — fix the\r\n  diagram's content before worrying about markup.\r\n\r\nThe tag list above is enforced by the server at render time; section (e) ships a validator that\r\nchecks a draft against it before you write.\r\n\r\n## (d) The rule that matters most: if a phrase says it faster, write the phrase\r\n\r\nThis skill exists to hold back drawing as much as to enable it. A diagram costs the author time to\r\nbuild and the reader time to parse; prose costs neither when it already says the same thing.\r\nReach for a diagram only when one of these holds:\r\n\r\n- the mechanism has **several distinct kinds of connection or state** that prose would have to\r\n  enumerate one at a time, and the picture shows them at a glance instead;\r\n- there is a genuine **divergence between two states or two paths** that is easier to see\r\n  side-by-side than to describe in sequence;\r\n- it is a flow where the **order of branching** itself is the point (which check happens before\r\n  which).\r\n\r\nDo NOT draw when:\r\n\r\n- it is a **linear sequence** — \"A, then B, then C\" is a list, not a picture;\r\n- it is **two boxes and one arrow** — that is a sentence: \"A calls B\";\r\n- it would be an **\"architecture diagram\" for its own sake** — solidity is not a reason;\r\n- it would just **redraw what the paragraph above it already said** — a diagram that repeats prose\r\n  instead of adding to it has not earned its place.\r\n\r\nWhen in doubt, write the caption sentence first. If the sentence is complete and clear on its own,\r\nyou very likely do not need the picture under it.\r\n\r\n## (e) Verify before you write\r\n\r\nEverything in (c) is enforced only at render time — on the server, after the body is already\r\nwritten — and a wired project has no PetBox sources to read. So this skill ships its own check:\r\nsave the block below as `validate-body.mjs` next to your draft and run it BEFORE writing the body.\r\nExit 0 = clean; exit 1 = every author-facing violation, with line numbers. It checks the SVG tag\r\nallowlist, the forbidden tags, the local-only `href`/`url(#id)` reference rules, and the GFM red\r\nflags the renderer mangles (a literal backslash-n in prose, `==highlight==` / `--pseudo--`\r\nheadings). It deliberately does NOT replicate the sanitizer's output transforms (id suffixing and\r\nfriends) — those are not author-facing rules. Code fences and inline code are excluded from every\r\ncheck (code never reaches the sanitizer).\r\n\r\n````js\r\n// validate-body.mjs — author-side self-check for a PetBox node/comment body draft.\r\n//\r\n// Zero dependencies, no PetBox sources needed. Usage:  node validate-body.mjs <draft.md>\r\n// Exit: 0 = clean, 1 = violations listed below, 2 = bad usage.\r\n//\r\n// The two constants below are parsed by a drift-guard test in the PetBox repo (which owns the\r\n// real sanitizer) — keep them machine-parseable: single line, double-quoted, this exact shape.\r\n\r\nimport { readFileSync } from \"node:fs\";\r\n\r\nconst ALLOWED_TAGS = \"svg g path rect circle ellipse line polyline polygon text tspan marker defs title desc use\";\r\nconst FORBIDDEN_TAGS = \"script style foreignObject image\";\r\n\r\nconst allowed = new Set(ALLOWED_TAGS.split(\" \"));\r\nconst forbidden = new Set(FORBIDDEN_TAGS.split(\" \"));\r\nconst urlFnAttrs = [\"fill\", \"stroke\", \"marker-start\", \"marker-mid\", \"marker-end\"];\r\n\r\nconst file = process.argv[2];\r\nif (!file) {\r\n  console.error(\"usage: node validate-body.mjs <draft.md>\");\r\n  process.exit(2);\r\n}\r\nconst raw = readFileSync(file, \"utf8\");\r\n\r\n// Code never reaches the sanitizer, so it is excluded from every check below. Fenced blocks and\r\n// inline code spans are blanked WITHOUT deleting their newlines, so reported line numbers stay true.\r\nconst blank = (m) => m.replace(/[^\\n]/g, \" \");\r\nconst text = raw\r\n  .replace(/^(```|~~~).*?^\\1.*$/gms, blank)\r\n  .replace(/`[^`\\n]+`/g, blank);\r\n\r\nconst problems = [];\r\nconst lineAt = (idx) => raw.slice(0, idx).split(\"\\n\").length;\r\nconst flag = (idx, msg) => problems.push(\"line \" + lineAt(idx) + \": \" + msg);\r\n\r\n// A literal backslash-n in prose renders as the letter n glued to whatever came before it —\r\n// use a real blank line for a paragraph break (fenced code blocks are excluded above).\r\nfor (const m of text.matchAll(/\\\\n/g))\r\n  flag(m.index, 'a literal backslash-n in prose renders as mush — use a real blank line (\"\\\\n\" is not a line break)');\r\n\r\n// ==highlight== / --pseudo-heading-- : the renderer has no such syntax; the markers survive\r\n// literally or the line mangles. Use a ## heading.\r\nfor (const m of text.matchAll(/^(==[^=\\n]+==|--[^-\\n]+--)\\s*$/gm))\r\n  flag(m.index, '\"' + m[1] + '\" is a pseudo-heading — the renderer has no such syntax; use a ## heading');\r\n\r\n// Forbidden tags are stripped — WITH their contents — wherever they appear, inside or outside <svg>.\r\nfor (const m of text.matchAll(/<(script|style|foreignObject|image)[\\s/>]/gi))\r\n  flag(m.index, \"<\" + m[1] + \"> is forbidden — the renderer strips it and its contents outright\");\r\n\r\n// Walk the tags inside <svg>...</svg> regions and check each against the allowlist and the\r\n// local-only reference rules. Tags outside <svg> are ordinary (sanitized) HTML, not diagram rules.\r\nconst tagRx = /<(\\/?)([a-zA-Z][a-zA-Z0-9:-]*)((?:\"[^\"]*\"|'[^']*'|[^>\"'])*)>/g;\r\nlet svgDepth = 0;\r\nfor (const m of text.matchAll(tagRx)) {\r\n  const [whole, slash, name, attrs] = m;\r\n  const tag = name.toLowerCase();\r\n  if (slash) {\r\n    if (tag === \"svg\") svgDepth = Math.max(0, svgDepth - 1);\r\n    continue;\r\n  }\r\n  const insideSvg = svgDepth > 0;\r\n  if (tag === \"svg\" && !whole.endsWith(\"/>\")) svgDepth++;\r\n  if (!insideSvg || forbidden.has(tag)) continue; // forbidden is reported by the whole-text scan\r\n\r\n  if (!allowed.has(tag))\r\n    flag(m.index, \"<\" + name + \"> is not in the SVG allowlist — it is stripped at render time. Allowed: \" + ALLOWED_TAGS);\r\n\r\n  // href / xlink:href on an SVG element must be a fragment IN THIS DOCUMENT (an external SVG\r\n  // reference is a real attack class; the sanitizer nulls anything not starting with #).\r\n  for (const h of attrs.matchAll(/((?:xlink:)?href)\\s*=\\s*(?:\"([^\"]*)\"|'([^']*)')/gi)) {\r\n    const value = h[2] ?? h[3];\r\n    if (value !== \"\" && !value.startsWith(\"#\"))\r\n      flag(m.index, h[1] + '=\"' + value + '\" on <' + name + \"> is an external reference — only a local #fragment survives\");\r\n  }\r\n\r\n  // fill/stroke/marker-* may carry ONLY an exactly-local url(#id); anything else loses the whole\r\n  // attribute. No paint-server element (gradient/pattern/filter) is on the allowlist, so there is\r\n  // nothing else for a url() to legitimately point at.\r\n  for (const attr of urlFnAttrs) {\r\n    const v = new RegExp(attr + '\\\\s*=\\\\s*(?:\"([^\"]*)\"|\\'([^\\']*)\\')', \"i\").exec(attrs);\r\n    const value = v && (v[1] ?? v[2]);\r\n    if (value && /url\\(/i.test(value) && !/^url\\(#[A-Za-z][\\w:.-]*\\)$/.test(value.trim()))\r\n      flag(m.index, attr + '=\"' + value + '\" carries a non-local url(...) — the whole attribute is stripped unless it is exactly url(#id)');\r\n  }\r\n}\r\n\r\nif (problems.length === 0) {\r\n  console.log(\"OK — no author-facing violations in \" + file);\r\n  process.exit(0);\r\n}\r\nconsole.error(problems.length + \" violation(s) in \" + file + \":\");\r\nfor (const p of problems) console.error(\"  \" + p);\r\nprocess.exit(1);\r\n````\r\n\r\nRun it with any Node 18+: `node validate-body.mjs draft-body.md`.\r\n\r\n**Tool naming:** base verbs are underscore-delimited (`tasks_upsert`); opencode prefixes\r\n`petbox_`, Claude Code prefixes `mcp__petbox__`.\n\nBase directory for this skill: C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-node-authoring\nRelative paths in this skill (e.g., scripts/, reference/) are relative to this base directory.\nNote: file list is sampled.\n\n<skill_files>\n\n</skill_files>\n</skill_content>","metadata":{"name":"petbox-node-authoring","dir":"C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-node-authoring","truncated":false},"title":"Loaded skill: petbox-node-authoring","time":{"start":1788801688724,"end":1788801688745}},"id":"prt_07ce3a4260019rrf8gpHjkFGT8","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce39242001stAyAOFxW2izj8"}}
{"type":"tool_use","timestamp":1788801688977,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"skill","callID":"call_02_EkNBn6DKmTRIwA5c1uhS7149","state":{"status":"completed","input":{"name":"petbox-card-check"},"output":"<skill_content name=\"petbox-card-check\">\n# Skill: petbox-card-check\n\n# Card check — the ask, and the result against it\r\n\r\nProcess discipline holds on its own: instructions get followed, MCP calls get made, gates\r\nget run. What slips silently is *content* — a card that never said what \"done\" means, or a\r\ntask finished cleanly that covers less than was asked, with no error anywhere to trip on.\r\n\r\n**What this catches, and what it does not.** Run by the same head that did the work, the\r\nbullet walk below reliably catches *\"not fully done\"* — a promise from the card simply\r\nabsent from the diff. On that same head it is weak on *\"not the right thing\"*, because the\r\nhead that misread the ask will misread it again; the FOR step is what catches that, and it\r\nis worth far more on a *different* head than the one that wrote the code. Nothing here\r\ntriggers automatically — no status, no hook. The caller decides.\r\n\r\n## Moment A — before the card is sent (the card's AUTHOR, not the worker)\r\n\r\nYou cannot check a result against a card that never committed to anything. Before a card\r\ngoes to a worker, it must carry, in the owner's own words:\r\n\r\n- **What closes it** — concrete enough that its absence from a diff would be visible.\r\n- **What is NOT in it** — the boundary, so scope creep and a gap are distinguishable later.\r\n- **One sentence of what it is FOR** — what the owner can do afterwards that they cannot\r\n  do now. This is the sentence the last step scores against; without it there is nothing\r\n  to score.\r\n\r\nBullets must be the owner's ask, not your paraphrase of it. A paraphrase is where the gap\r\nis introduced — quietly, before any code exists. **No such section: the card does not go\r\ninto work.** Fixing the card is cheaper than every step below.\r\n\r\n## Moment B, entry — before writing any code (the worker)\r\n\r\nWrite down 3-5 bullets of what will be done, concrete and in the card's own language, plus\r\nthe card's out-of-scope list close to verbatim. Do this *before* coding: a misreading then\r\ncosts one short pass; after the work is done it costs the work.\r\n\r\n## Moment B, exit — the mechanics\r\n\r\nThree questions, each answered by a command whose raw output goes into the report\r\n**verbatim**. The verdict is read off that pasted output, never recalled from memory —\r\nremembering what you did is the failure mode this whole procedure exists to survive.\r\n\r\n1. **Do the artifacts the card names exist**, at the paths and names the card used?\r\n2. **Did the gate the card names actually run green** — the real exit code, never a claim\r\n   in a chat message.\r\n3. **Does the diff touch what the card named?**\r\n   `git diff --numstat <base>...HEAD -- <paths from the card>`\r\n\r\nTwo things about that command, both learned the hard way:\r\n\r\n- Compare on **`--numstat`, never `--stat`**. `--stat` abbreviates a long path to a\r\n  `.../` prefix, so a file you did touch reads as untouched and a real gap is scored\r\n  COVERED (commit `0080c198`). Print `--stat` for humans if you like; never compare on it.\r\n- The range is **three-dot** `<base>...HEAD` — from the merge-base, so commits that landed\r\n  on the base after you branched are not yours to explain. Base defaults to `origin/main`,\r\n  which must be fetched first. If the diff command *errors*, that is a finding in itself —\r\n  report it as one, never as \"no diff\".\r\n\r\n## Moment B, exit — the bullets\r\n\r\nWalk the entry list against the real diff. Each bullet gets exactly one word:\r\n\r\n- **COVERED**\r\n- **NOT COVERED** — including partial. Never soften a partial into \"mostly covered\".\r\n- **EXTRA** — done beyond the card; check it against the out-of-scope list.\r\n\r\nOne line per bullet, flat. **If anything is NOT COVERED, that is the report's headline**,\r\nquoted from the card — not folded into a summary that opens with \"looks good\".\r\n\r\n## Moment B, exit — the FOR step\r\n\r\nTake Moment A's one-sentence \"what it is FOR\" and answer it against the result: **yes or\r\nno, plus one line of evidence**. Every bullet can be COVERED and the answer still be no —\r\nthat is precisely the \"done, but not the thing\" case, and it is the only step that sees it.\r\n\r\n## Escalation — only on an UNEXPLAINED gap\r\n\r\nA NOT COVERED bullet with a documented reason (the card's own out-of-scope section, or the\r\nowner descoping it in the thread) is a note, not an escalation. A NOT COVERED bullet the\r\ncard plainly asked for, with no such reason, is a real finding. This skill surfaces the\r\ngap; it does not act on it and it never calls the reserve itself — the caller decides\r\nbetween fix-now, report to the owner, and escalate.\r\n\r\n## What the output looks like\r\n\r\n```\r\nNOT COVERED  README \"What it installs\" — parity test names it; numstat shows README.md untouched\r\nCOVERED      template exists at templates/petbox-card-check/SKILL.md\r\nCOVERED      PROJECT_SKILLS row added — numstat: 2 1 src/.../skill-files.ts\r\nEXTRA        removed the stale .gitignore line for the old name (not asked; in scope)\r\nFOR: no — the kit still fails its own parity gate, so nothing ships to a project yet.\r\n```\n\nBase directory for this skill: C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-card-check\nRelative paths in this skill (e.g., scripts/, reference/) are relative to this base directory.\nNote: file list is sampled.\n\n<skill_files>\n\n</skill_files>\n</skill_content>","metadata":{"name":"petbox-card-check","dir":"C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-card-check","truncated":false},"title":"Loaded skill: petbox-card-check","time":{"start":1788801688952,"end":1788801688975}},"id":"prt_07ce3a4e0001XHf5G16zE4pF2n","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce39242001stAyAOFxW2izj8"}}
{"type":"step_finish","timestamp":1788801689016,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3a5ae001yVyy6ooF5PmXEm","reason":"tool-calls","messageID":"msg_07ce39242001stAyAOFxW2izj8","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":52850,"input":169,"output":109,"reasoning":92,"cache":{"write":0,"read":52480}},"cost":0.000438625}}
{"type":"step_start","timestamp":1788801689793,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3a8bf001nO6IZaKETlHn94","messageID":"msg_07ce3a5b50019DDw0gjSJDwTAp","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801691953,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"petbox_tasks_node_get","callID":"call_00_RoVAXOZ5QUfeckCfMWuQ7917","state":{"status":"completed","input":{"board":"s5-work","node":"obzor-op81677"},"output":"{\"nodes\":[{\"board\":\"s5-work\",\"kind\":\"simple\",\"node\":{\"key\":\"obzor-op81677\",\"nodeId\":\"24bfceeb89a44a69be5b13b21c928f09\",\"depth\":0,\"status\":\"Todo\",\"type\":\"task\",\"title\":\"Обзор процесса код-ревью\",\"body\":\"Карточка для сбора материалов по процессу код-ревью в команде разработки.\",\"commits\":[],\"priority\":0,\"version\":18,\"renamedFrom\":[],\"tags\":[],\"createdAt\":\"2026-09-07T17:21:18.13\",\"updatedAt\":\"2026-09-07T17:21:18.13\",\"decisionPending\":false,\"originSessionId\":\"\",\"originSessions\":[]},\"ancestors\":[],\"relations\":[]}]}","metadata":{"truncated":false},"title":"","time":{"start":1788801691841,"end":1788801691943}},"id":"prt_07ce3afd7001OTUz5Stixaekd6","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce3a5b50019DDw0gjSJDwTAp"}}
{"type":"step_finish","timestamp":1788801691953,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3b129001HS4I8S7ct0k0Oq","reason":"tool-calls","messageID":"msg_07ce3a5b50019DDw0gjSJDwTAp","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":59628,"input":6772,"output":70,"reasoning":50,"cache":{"write":0,"read":52736}},"cost":0.003241388}}
{"type":"step_start","timestamp":1788801692792,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3b477001vCXABDBcSU5W4N","messageID":"msg_07ce3b12e001ozT5mgUqfaPXJt","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801695304,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"skill","callID":"call_00_zhSicgDvioxszBYFbjIv2594","state":{"status":"completed","input":{"name":"petbox-write-economy"},"output":"<skill_content name=\"petbox-write-economy\">\n# Skill: petbox-write-economy\n\n# Write economy: pay for the change, not the text\r\n\r\nA write call's cost to you is the tokens it takes to PRODUCE the arguments, not the bytes PetBox\r\nstores. The four techniques below each remove one way that cost gets inflated past the size of\r\nthe actual change. Section (c) carries the one rule that is unconditional — read it first if you\r\nare writing anything that is not plain ASCII.\r\n\r\n## (a) `bodyRef` — a body that already exists, by reference\r\n\r\nUpload text once, then pass a short reference instead of retyping the text into the call:\r\n\r\n```\r\ncurl -sS -X POST \"https://petbox.3po.su/api/blobs/probe-m1-sandbox\" \\\r\n  --data-binary @report.md \\\r\n  -H \"X-Api-Key: $PETBOX_API_KEY\"\r\n```\r\n```\r\n# PowerShell\r\nInvoke-RestMethod -Method Post -Uri \"https://petbox.3po.su/api/blobs/probe-m1-sandbox\" `\r\n  -InFile report.md -Headers @{ \"X-Api-Key\" = $env:PETBOX_API_KEY }\r\n```\r\n\r\nThe RAW file bytes are the request body — no JSON envelope, no multipart. The response is\r\n`{ ref, bytes, chars, expiresAt }`, `ref` looking like `blob-<32 hex chars>`. Then:\r\n\r\n```\r\ntasks_upsert(projectKey:\"probe-m1-sandbox\", nodes:[{ key:\"...\", bodyRef:\"blob-<ref>\" }])\r\n```\r\n\r\n- **Ceiling 10 MB.** **One-shot**: consumed by the write that references it (a REFUSED write does\r\n  not consume it — retry with the same ref); an unconsumed blob expires 24h after upload.\r\n- **Scoped to the project it was uploaded into**, and to **one item's body**, not one call — a\r\n  batch writing N nodes needs N separate blobs, one per item.\r\n- **Mutually exclusive with `body` and `fragment`.** Sending two of them is a REFUSAL (through\r\n  `conflicts[]` on the batch verbs), never a silent precedence.\r\n- **Where it is accepted** (check the field name — it always mirrors the field it replaces):\r\n  - `tasks_upsert` — `bodyRef` alongside `body`\r\n  - `comments_upsert` — `bodyRef` alongside `body`\r\n  - `memory_upsert` — `bodyRef` alongside `body`\r\n  - `memory_remember` — `textRef` (this verb's field is `text`, not `body`)\r\n  - `session_append` — `contentRef`, **per message** (this verb's field is `content`; a batch may\r\n    mix one referenced message with several inline ones)\r\n- **Upload scope:** `tasks:write` OR `memory:write` — no separate upload scope exists, because\r\n  uploading only ever grants a prefix of what one of those already grants.\r\n\r\n## (b) `fragment` — patch, don't retype\r\n\r\nA list of `{ old, new }` edits, applied **in order** against the CURRENT body (PATCH only — a\r\ncreate has no current body to patch against):\r\n\r\n```\r\ntasks_upsert(projectKey:\"probe-m1-sandbox\", nodes:[{\r\n  key:\"...\", version:<n>,\r\n  fragment:[{ old:\"the exact text to replace\", new:\"its replacement\" }]\r\n}])\r\n```\r\n\r\n- Each `old` must occur **exactly once** in the current text. Zero matches or two-or-more matches\r\n  is a REFUSAL through `conflicts[]` naming the count — never a first-match guess, never a\r\n  partial apply. Re-read the body and rebase the edit.\r\n- `new` is required; `\"\"` deletes the matched text.\r\n- Mutually exclusive with `body`.\r\n- Available on `tasks_upsert`, `comments_upsert`, `memory_upsert`. Not on `memory_remember`\r\n  (create-only) or `session_append` (no patch verb on that surface).\r\n\r\n## (c) Raw UTF-8 in the call — never `\\uXXXX`\r\n\r\nEvery `\\uXXXX` escape prints **six characters** for one, and that cost is paid out of the\r\n**model's own output budget**, not the wire. A long non-ASCII call (Cyrillic, CJK, …) can exhaust\r\nthat budget mid-generation — the call never finishes, and what the server sees (if anything) is a\r\nparse error, not a large-but-valid body. This is not something PetBox can detect or fix from the\r\nserver side: by the time an escaped call is small enough to arrive intact, the damage already\r\nhappened upstream.\r\n\r\nThis is exactly why (a) and (b) exist as mechanisms rather than as a rule to remember: the same\r\nwarning has lived in tool descriptions and project canon for a long time and keeps recurring\r\nanyway.\r\n\r\n**Default rule — non-ASCII prose does not go in the call.** A body or comment carrying Cyrillic,\r\nCJK or any other non-ASCII text goes through `bodyRef` (a) when it is new, `fragment` (b) for a\r\nLOCALIZED edit, and `bodyRef` again for an edit that rewrites most of the body — `bodyRef` is\r\naccepted on a PATCH (pass it with the `version` baseline), and on a rewrite `fragment` is the\r\nworse path: every `old` slice is non-ASCII text retyped into the call, so the escape hazard lands\r\non `old` as well as `new`, and each slice must still match exactly once.\r\nNot \"when it is large\" and not \"when the text already exists on disk\" — ALWAYS.\r\nToken cost is not the reason; reliability is. A `body` argument is the one path where the prose\r\nmust survive the model's own JSON encoding, and that path fails silently: the call is truncated\r\nmid-generation and arrives as a parse error naming no size. Writing the text to a file with a\r\nshell heredoc and uploading it costs one extra call and removes that failure mode completely —\r\n**only if the delimiter is quoted** (`<<'EOF'`, never bare `<<EOF`). An unquoted delimiter puts\r\nthe heredoc BODY through shell substitution, and Cyrillic/CJK markdown is exactly the content\r\nmost likely to carry `$`, backticks or backslashes that trigger it — reintroducing the same\r\nclass of silent corruption this technique exists to remove.\r\nJudging this on token cost alone is the known trap — it reads as \"the upload buys nothing here\"\r\nand walks straight back into the truncation.\r\n\r\n## (d) Reading cheaply — the `bodyLen` contract\r\n\r\nEvery body-carrying read/search/echo shares one knob, `bodyLen`: `0` = no body, `N > 0` = the\r\nfirst N chars (`\"…\"` when cut), `-1` = the full body. (What an *omitted* `bodyLen` defaults to\r\nvaries by surface — a listing/search snippet, a compact write-ack, or a full pointed read — each\r\ntool's own description says which.)\r\n\r\nCheap path: search/list with `bodyLen:0`, look at the row identities, then fetch the 1-3 rows you\r\nactually need in full (`tasks_node_get` / `memory_get` / `comments_get`). Reserve `bodyLen:-1` for\r\nwhen you already know the few keys you want — pulling full bodies across a wide result set \"just\r\nin case\" routinely spends a large share of the response budget on text nobody reads.\r\n\r\n**Tool naming:** base verbs are underscore-delimited (`tasks_upsert`); opencode prefixes\r\n`petbox_`, Claude Code prefixes `mcp__petbox__`.\n\nBase directory for this skill: C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-write-economy\nRelative paths in this skill (e.g., scripts/, reference/) are relative to this base directory.\nNote: file list is sampled.\n\n<skill_files>\n\n</skill_files>\n</skill_content>","metadata":{"name":"petbox-write-economy","dir":"C:\\Users\\stdray\\AppData\\Local\\Temp\\petbox-probe-ws\\.claude\\skills\\petbox-write-economy","truncated":false},"title":"Loaded skill: petbox-write-economy","time":{"start":1788801695275,"end":1788801695303}},"id":"prt_07ce3bd99001JzE9peaOdWqF4c","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce3b12e001ozT5mgUqfaPXJt"}}
{"type":"step_finish","timestamp":1788801695326,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3be56001loMSmwFZKPPSNR","reason":"tool-calls","messageID":"msg_07ce3b12e001ozT5mgUqfaPXJt","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":59956,"input":298,"output":49,"reasoning":89,"cache":{"write":0,"read":59520}},"cost":0.00046545}}
{"type":"step_start","timestamp":1788801696147,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3c191001iEd3CdhpRiLP74","messageID":"msg_07ce3be5b001EBeDBC6SmpFLtq","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801702041,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"bash","callID":"call_00_ZpeJt5jYpK7LdlGeWxOz9726","state":{"status":"completed","input":{"command":"Get-ChildItem Env: | Where-Object { $_.Name -match 'PETBOX|API' } | Format-Table -AutoSize Name,Value"},"output":"\r\nName                 Value\r\n----                 -----\r\nPETBOX_PROBE_API_KEY <REDACTED>\r\n\r\n","metadata":{"output":"\r\nName                 Value\r\n----                 -----\r\nPETBOX_PROBE_API_KEY <REDACTED>\r\n\r\n","exit":0,"truncated":false},"title":"Get-ChildItem Env: | Where-Object { $_.Name -match 'PETBOX|API' } | Format-Table -AutoSize Name,Value","time":{"start":1788801701648,"end":1788801702026}},"id":"prt_07ce3d5ab001HrLMB04NSiiZZH","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce3be5b001EBeDBC6SmpFLtq"}}
{"type":"step_finish","timestamp":1788801702041,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3d88b0016XBp69tI343WDg","reason":"tool-calls","messageID":"msg_07ce3be5b001EBeDBC6SmpFLtq","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":62281,"input":2037,"output":76,"reasoning":264,"cache":{"write":0,"read":59904}},"cost":0.001399047}}
{"type":"step_start","timestamp":1788801703082,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce3dca800167BiIij52dLig0","messageID":"msg_07ce3d890001bx0kW9qNuqmgLc","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801755145,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"write","callID":"call_00_YgZDYo0qT4AV9CNo6al30951","state":{"status":"completed","input":{"filePath":"C:\\Users\\stdray\\AppData\\Local\\Temp\\opencode\\code-review-obzor.md","content":"## Зачем нужно код-ревью\n\nКод-ревью — это не формальная проверка «чтобы галочку поставить», а единственная точка, где изменение смотрят свежим взглядом до того, как оно попадёт в основную ветку и начнёт влиять на всех. Автор изменения слишком долго живёт внутри своей задачи: он знает, какие допущения он сделал, какие места он сознательно оставил «на потом» и какие углы срезал, потому что спешил. Ревьюер этого не знает — и именно поэтому замечает то, что автор перестал видеть. Отсюда четыре причины, ради которых процесс вообще существует.\n\nПервая — качество и надёжность. Ошибка, пойманная на ревью, стоит в разы дешевле ошибки, пойманной в проде: там её находит клиент, и к стоимости исправления добавляется стоимость разбора инцидента, отката, общения с пользователями. Ревью — самый дешёвый фильтр, который у команды есть.\n\nВторая — общее владение кодом. Когда каждый кусок кода видели минимум два человека, в команде не образуется «бутылочных горлышек» — людей, единолично знающих какой-то модуль. Если единственный автор уходит в отпуск или увольняется, модуль не становится чёрным ящиком.\n\nТретья — обучение и выравнивание. Ревью — это постоянный обмен приёмами. Младший разработчик видит, как старшие думают о граничных случаях и архитектуре; старшие замечают новые инструменты и подходы, которые приносят младшие. Стандарты не живут в документах — они живут в том, что обсуждается в пул-реквестах.\n\nЧетвёртая — история и консистентность. Комментарии на ревью фиксируют, *почему* было принято то или иное решение, и это остаётся в истории на будущее, когда через полгода кто-то спросит: «а почему тут так странно сделано?».\n\n## Как проходит изменение: этапы\n\nЖизнь изменения начинается не с пул-реквеста, а с задачи. Исполнитель берёт карточку, ветвится от актуального состояния основной ветки и работает в отдельной ветке, коммитя по ходу небольшими логическими порциями — это важно, потому что разбивка на осмысленные коммиты сильно упрощает ревью: ревьюер читает не «мешок изменений на 2000 строк», а последовательность шагов, каждый со своим обоснованием в сообщении коммита.\n\nКогда код готов и локально проверен (сборка, тесты, линт), автор открывает пул-реквест. Хороший пул-реквест — это самодокументируемая сущность: в описании указано, *что* и *зачем* делается, какие решения были спорными, что сознательно осталось за рамками и как это проверить вручную. Если описание пустое, ревьюер теряет контекст и начинает задавать вопросы, которые автору пришлось бы ответить и так.\n\nДальше начинается собственно ревью. Оно почти никогда не бывает одним заходом «прочитал — одобрил». Типичный цикл выглядит так: ревьюер оставляет замечания, автор отвечает на них и вносит правки, замечания закрываются по мере того, как правки их снимают, и только когда незакрытых существенных замечаний не остаётся, ревьюер одобряет. На этом этапе ревьюер проверяет не только то, что код работает, но и то, что на замечания действительно ответили — правкой или аргументом, а не молчаливым игнорированием.\n\nПосле одобрения и зелёных проверок изменение вливается в основную ветку. Тут возможны варианты в зависимости от договорённостей команды: где-то достаточно одного одобрения, где-то на чувствительные модули требуют двух; где-то вливает сам автор, где-то — назначенный «мердж-менеджер» дня. Важно одно: влитие — это не конец ответственности. Если после деплоя что-то ломается, автор изменения — первый, кто на это смотрит, а ревьюер участвует в разборе, потому что он поставил свою подпись.\n\n## Кто и что проверяет\n\nВ команде обычно выделяются три роли, хотя люди могут совмещать их.\n\nПервый ревьюер — это коллега-разработчик того же или соседнего домена. Он проверяет суть: корректность логики, покрытие граничных случаев, читаемость кода, соответствие принятым в проекте паттернам и соглашениям. Это самый глубокий слой ревью, потому что он про семантику.\n\nВторой слой — это автоматические проверки, которые стоят до человека: сборка, юнит- и интеграционные тесты, линтер, форматтер, статический анализ, проверка покрытия. Они отсеивают механические проблемы (неотформатировано, не скомпилировалось, упал тест) и освобождают человеческое внимание для того, что машина не видит. Принято, что пул-реквест с красными проверками человек вручную вообще не рассматривает — сначала зелёный CI, потом глаза.\n\nТретий слой — это при необходимости узкий эксперт: человек, отвечающий за безопасность, за базу данных, за производительность, за конкретный сервис. Его зовут точечно, когда изменение затрагивает критичную для него зону: миграция схемы, изменение модели прав доступа, публичный API, чувствительный к задержкам участок. Обычно такие ревью оформляются как обязательный аппрув от владельца кода, если файл помечен как «принадлежащий» этому владельцу.\n\nСам автор тоже участник процесса, а не пассивная сторона: он отвечает на замечания, объясняет мотивы, а если не согласен — спорит предметно, с аргументами, а не отбивается. Здоровое ревью — это диалог, а не суд.\n\n## Что именно смотрят на ревью\n\nЕсть устойчивый чек-лист, по которому ходит опытный ревьюер. Первое — корректность: делает ли код то, что заявлено в задаче, и не ломает ли он то, что было раньше. Второе — граничные случаи: пустые входы, нулевые значения, переполнения, повторные вызовы, конкурентный доступ. Третье — дизайн и архитектура: на своём ли месте лежит код, не вырос ли новый «божественный объект», не нарушена ли инверсия зависимостей, нет ли дублирования, которое уже существует где-то ещё. Четвёртое — читаемость и именование: поймёт ли человек, не видевший задачу, что здесь происходит, без десятка вопросов. Пятое — тесты: есть ли они вообще, покрывают ли они именно ту логику, которая менялась, проверяют ли они поведение, а не реализацию. Шестое — побочные эффекты: производительность, безопасность, логирование, совместимость с предыдущими версиями данных и API. Седьмое — доменные соглашения: консистентность стиля, обработка ошибок, работа с конфигурацией, использование общих утилит вместо «своего велосипеда».\n\nКритично здесь не зацикливаться на мелочах. Различают «блокеры» — то, что реально может сломать или завести в тупик, и «ниты» — косметические замечания, которые хорошо бы поправить, но не обязательно. Хорошая практика — явно помечать замечания: «обязательно исправить», «желательно», «просто вопрос». Это снимает половину конфликтов на ревью.\n\n## Типичные проблемы и как их чинят\n\nСамые частые находки на ревью укладываются в несколько категорий.\n\nВо-первых, дублирование. Кто-то написал логику, которая уже есть в другом модуле, но не нашёл её. Лечится выносом в общую утилиту и заменой обоих вхождений — иногда это отдельная маленькая задача, а не правка внутри текущей.\n\nВо-вторых, «божественные объекты» и разросшиеся функции: класс или метод, который делает слишком много, разросся до состояния, когда его невозможно тестировать и понимать. Лечится декомпозицией — разбиением на меньшие функции и классы с ясной ответственностью. Это правка не на пять минут, поэтому часто фиксируется как обязательное замечание перед мерджем либо как отдельная задача-«долг», если рефакторинг тянет на самостоятельный объём.\n\nВ-третьих, неправильные абстракции и преждевременная оптимизация: код, написанный «на вырост», с кучей индирекции ради гипотетических будущих требований. Лечится упрощением до того, что реально нужно сейчас — правило «не обобщай, пока не появился второй похожий случай».\n\nВ-четвёртых, отсутствие или слабость тестов: изменение логики без теста, либо тест, который проверяет не поведение, а детали реализации и потому ломается от любой мелочи. Лечится добавлением тестов на сценарии, а не на внутренности.\n\nВ-пятых, ошибки в обработке граничных случаев и исключений: молча проглатываемые ошибки, непроверяемые nullable-значения, отсутствие таймаутов на внешних вызовах, неверные проверки условий. Это классика «всё работает, пока не происходит то, что не должно происходить».\n\nВ-шестых, проблемы именования и читаемости: короткие или вводящие в заблуждение имена, переменные, хранящие не то, что обещает имя, комментарии, которые устарели и лгут. Лечится переименованием и удалением мусора.\n\nВажно, как именно чинят: правки вносятся в том же пул-реквесте, обычно отдельными коммитами с понятными сообщениями, чтобы ревьюер мог посмотреть только изменение, а не перечитывать всё заново. Если замечание спорное и диалог зашёл в тупик, а не решается аргументами в тексте, принято выносить его в короткий живой разговор — это быстрее, чем десять раундов переписки. Если замечание верное, но слишком объёмное, его не молча забывают: заводят отдельную задачу-«долг» и ссылаются на неё из пул-реквеста, чтобы улучшение не потерялось.\n\n## Культура и инструменты\n\nПроцесс держится не только на регламенте, но и на культуре. Замечания формулируются про код, а не про человека: «здесь можно упасть на пустом списке» вместо «ты опять не проверил пустой список». Ревью — это разговор о том, как сделать вещь лучше, а не соревнование, кто найдёт больше ошибок. Время отклика имеет значение: затянутое ревью блокирует автора, поэтому есть договорённость о том, как быстро замечания даются в первый раз — обычно в пределах рабочего дня.\n\nИз инструментов используются стандартные вещи: система пул-реквестов с обсуждением по строкам, CI-пайплайн с автоматическими проверками, линтер и форматтер, чтобы механические споры о стиле вообще не возникали, и — в продвинутых командах — автоматический подсвет изменений и «обязательные ревьюеры» для критичных зон кода. Но инструменты — это только каркас. Работает процесс тогда, когда каждый участник понимает, зачем он здесь: поймать ошибку дёшево, распределить знание по команде и оставить после себя код, в который следующему будет не страшно войти.\n"},"output":"Wrote file successfully.","metadata":{"diagnostics":{},"filepath":"C:\\Users\\stdray\\AppData\\Local\\Temp\\opencode\\code-review-obzor.md","exists":false,"truncated":false},"title":"Users\\stdray\\AppData\\Local\\Temp\\opencode\\code-review-obzor.md","time":{"start":1788801755136,"end":1788801755143}},"id":"prt_07ce3e6800019fppz6XGIhIlTX","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce3d890001bx0kW9qNuqmgLc"}}
{"type":"step_finish","timestamp":1788801755198,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4a836001amp0FJ48mpm6jU","reason":"tool-calls","messageID":"msg_07ce3d890001bx0kW9qNuqmgLc","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":65437,"input":128,"output":3014,"reasoning":87,"cache":{"write":0,"read":62208}},"cost":0.002979054}}
{"type":"step_start","timestamp":1788801756014,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4ab6c001hHzEgsfbl6rmv5","messageID":"msg_07ce4a83b001nOqicWHKzmct36","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801758838,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"bash","callID":"call_00_6dFh5QuCFWfBNR83Jjxx0074","state":{"status":"completed","input":{"command":"Invoke-RestMethod -Method Post -Uri \"https://petbox.3po.su/api/blobs/probe-m1-sandbox\" -InFile \"C:\\Users\\stdray\\AppData\\Local\\Temp\\opencode\\code-review-obzor.md\" -Headers @{ \"X-Api-Key\" = $env:PETBOX_PROBE_API_KEY } | ConvertTo-Json"},"output":"{\r\n  \"ref\": \"blob-f7676cf81c8aab0ee78c499b38e57830\",\r\n  \"bytes\": 17655,\r\n  \"chars\": 9691,\r\n  \"expiresAt\": \"2026-09-08T17:22:39.0588187Z\"\r\n}\r\n","metadata":{"output":"{\r\n  \"ref\": \"blob-f7676cf81c8aab0ee78c499b38e57830\",\r\n  \"bytes\": 17655,\r\n  \"chars\": 9691,\r\n  \"expiresAt\": \"2026-09-08T17:22:39.0588187Z\"\r\n}\r\n","exit":0,"truncated":false},"title":"Invoke-RestMethod -Method Post -Uri \"https://petbox.3po.su/api/blobs/probe-m1-sandbox\" -InFile \"C:\\Users\\stdray\\AppData\\Local\\Temp\\opencode\\code-review-obzor.md\" -Headers @{ \"X-Api-Key\" = $env:PETBOX_PROBE_API_KEY } | ConvertTo-Json","time":{"start":1788801758272,"end":1788801758823}},"id":"prt_07ce4b189001CSo40ErYQyMU6u","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce4a83b001nOqicWHKzmct36"}}
{"type":"step_finish","timestamp":1788801758838,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4b669001mKVBIkNmHS6RfI","reason":"tool-calls","messageID":"msg_07ce4a83b001nOqicWHKzmct36","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":65605,"input":45,"output":127,"reasoning":25,"cache":{"write":0,"read":65408}},"cost":0.000388919}}
{"type":"step_start","timestamp":1788801759676,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4b9b4001sCsxqn0QbcI2Y0","messageID":"msg_07ce4b66d001hnsW6fFoX9ODc8","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"tool_use","timestamp":1788801761328,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"type":"tool","tool":"petbox_comments_upsert","callID":"call_00_ET_rq3pzqlVz0olvj5RA5Jr8793","state":{"status":"completed","input":{"board":"s5-work","items":[{"node":"obzor-op81677","author":"opencode","bodyRef":"blob-f7676cf81c8aab0ee78c499b38e57830"}]},"output":"{\"applied\":true,\"currentVersion\":18,\"added\":[{\"id\":\"bda960f7260a44d0863f8e1ed9805ae3\",\"nodeId\":\"24bfceeb89a44a69be5b13b21c928f09\",\"author\":\"opencode\",\"body\":\"\",\"tags\":[],\"version\":18,\"created\":\"2026-09-07T17:22:41.578\",\"updated\":\"2026-09-07T17:22:41.578\"}],\"updated\":[],\"removed\":[],\"conflicts\":[]}","metadata":{"truncated":false},"title":"","time":{"start":1788801761208,"end":1788801761319}},"id":"prt_07ce4bd48001ZxNsftYg7QjBAA","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","messageID":"msg_07ce4b66d001hnsW6fFoX9ODc8"}}
{"type":"step_finish","timestamp":1788801761328,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4c029001tbVUl0B2CDshkb","reason":"tool-calls","messageID":"msg_07ce4b66d001hnsW6fFoX9ODc8","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":65810,"input":162,"output":112,"reasoning":0,"cache":{"write":0,"read":65536}},"cost":0.000405478}}
{"type":"step_start","timestamp":1788801762213,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4c3a4001vwTiwh7FE47R53","messageID":"msg_07ce4c02d001uOHK8bWK5T3ziB","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-start"}}
{"type":"text","timestamp":1788801763831,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4c693001ilvvY71mzqjktq","messageID":"msg_07ce4c02d001uOHK8bWK5T3ziB","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"text","text":"Комментарий добавлен к карточке `obzor-op81677` на доске `s5-work` (9691 символ, 8 подзаголовков: зачем ревью, этапы изменения, роли, что проверяют, типичные проблемы, культура и инструменты).","time":{"start":1788801762963,"end":1788801763823}}}
{"type":"step_finish","timestamp":1788801763831,"sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","part":{"id":"prt_07ce4c9f1001w4b0xQuQSDqKFR","reason":"stop","messageID":"msg_07ce4c02d001uOHK8bWK5T3ziB","sessionID":"ses_f831c6ef4ffeKq7664lXjxeCtb","type":"step-finish","tokens":{"total":66014,"input":154,"output":68,"reasoning":0,"cache":{"write":0,"read":65792}},"cost":0.000364646}}

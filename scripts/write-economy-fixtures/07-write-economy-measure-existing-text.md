Гейт для `write-body-by-reference`. По ToolCalls-логу посчитать, у какой доли больших тел текст к моменту вызова уже существовал вне вывода модели, и сколько тел прошло через выход дважды.

Требование фальсификации взято из самой идеи, а не придумано здесь. Малая доля — полноценный результат: `write-body-by-reference` закрывается как неокупающийся вектор.

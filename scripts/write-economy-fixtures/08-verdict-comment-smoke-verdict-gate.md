## Вердикт: правило + НАСТОЯЩИЙ гейт (этот комментарий его и проходит)

1. **AGENTS.md п. 6** — любой перевод в терминальный статус требует комментария `artifact:verdict`.
2. **Гейт в движке** — `preconditionArtifact: "verdict"` на всех терминальных переходах всех четырёх kind (quartet rules v9). Тот же механизм, что требует `spec_plan` на `exploring → review`.

### Проверено на ОДНОРАЗОВОМ инстансе, а не на живом quartet

Правила общие для ВСЕХ агентов — сломаешь их, сломаешь всем. Смоук-инстанс `smoke-verdict-gate` (снимок quartet) → три находки, каждая изменила результат:

- **Тег — `artifact:verdict`, а не `verdict`.** Голый `verdict` гейт НЕ удовлетворяет (напоролся).
- **КАСКАД ОБХОДИТ ГЕЙТ.** work → Done автоматически закрывает связанный intake-issue через эффект `issue_task` — без его вердикта. Воспроизведено. Поэтому в AGENTS.md дыра НАЗВАНА честно, а не замазана — врущее правило хуже отсутствующего. Заведён интейк `effects-bypass-precondition-artifact` (задумано или баг — решать владельцу).
- **Движок отказался применять правила к quartet** — и был прав: на борде `intake` лежало 8 карточек с типами `idea`/`bug`, хотя intake допускает только `issue` (дрейф данных до ужесточения правил). Нормализованы миграцией (`migrated: 8`).

### Доказательство, что гейт работает в обе стороны

Без артефакта: `transition 'InProgress' -> 'Cancelled' requires an artifact:verdict comment`. С артефактом — проходит. Смоук-инстанс закрыт.

Коммит `634a929`, в main как `6786af8`. quartet rules v9 — живые.

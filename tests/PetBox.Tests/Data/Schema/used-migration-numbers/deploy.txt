# used-migration-numbers registry — deploy tier (PetBox.Deploy.Data.Migrations)
#
# Append-only, one line per number ever assigned in this tier. Numbers must stay STRICTLY
# INCREASING top-to-bottom (that is both the uniqueness and the monotonic-growth invariant —
# a number is never reused, not even under a new name, and a gap is legal but never backfilled).
#
# Format: <number> <LIVE|BURNED> <ClassName> [# free-text reason, BURNED entries only]
#
# Enforced by MigrationNumberingTests (tests/PetBox.Tests/Data/Schema/MigrationNumberingTests.cs):
#   - the numbers column above is strictly increasing reading down the file
#   - the set of LIVE lines here exactly matches this tier's live [Migration] types, by
#     (number, ClassName) — a live migration missing here, or a LIVE line with no matching
#     migration in code, both fail
# New migration -> APPEND a line at the bottom with a number greater than every number above.
# Deleted migration -> flip its line to BURNED (keep the number, add a reason) instead of
# removing the line. See work/migration-number-reuse-is-silently-destructive.
1 LIVE M001_Deploy
2 LIVE M002_RunSpec
3 LIVE M003_CapabilitiesAndStatusError
4 LIVE M004_HostReport

---
timestamp: 2026-05-31T12:00:00+03:00
agent: claude-code
model: claude-opus-4-8
target: plan
action: create
target_file: C:\Users\stdray\.claude\plans\sequential-noodling-thimble.md
---

## What
Создан и утверждён план подготовки и проведения dogfooding-эксперимента Фазы 30 (валидация модулей Tasks/Sessions/Memory) + правил/таксономии памяти. План: Фаза A (предпосылки валидности), Фаза B (прогон + метрика комплаенса), Фаза C (документированные правила памяти). План строился через 3 раунда дискавери + сфокусированного критика Фазы 30 (3 линзы).

## Why
Пользователь дал brain-dump из 7 вопросов про практичность tasks/session/memory, правила памяти, актуальность dogfooding, форматы (TOON/.af), логи vs Console.WriteLine, формализацию уровней, и запуск критика. Ключевая находка: модули уже реализованы, но Фаза 30 помечена «отложено до валидации». Критик показал, что наивный прогон был бы невалиден (F2/F4 фейр-блокеры, хрупкий ручной DDL, substring-поиск смещает комплаенс).

## Args
Решения по развилкам: миграции внутренних тиров → FluentMigrator + Core-инварианты (DbUp только Data-тир); поиск памяти → FTS5 + sqlite-vec; MCP-поверхность → фильтр tools/list по scope + обрезка описаний; метадата → ambient через MCP headers. Отложено: оркестрация (F1/O1–O3), группировка (В6), TOON.

## Outcome
План утверждён пользователем (ExitPlanMode approved). Реализация начата с фиксации инвариантов в память. Фаза B требует рантайм-действий пользователя (PETBOX_DEV_APIKEY + сервер).

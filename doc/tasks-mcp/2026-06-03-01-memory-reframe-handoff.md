---
timestamp: 2026-06-03T00:00:00+03:00
agent: claude-code
model: claude-opus-4-8
target: plan
action: update
target_file: petbox tasks board=ideas node=memory (projectKey=$system)
---

## What
Правка тела идеи `memory`: добавлены блоки «Фактическое назначение» (реальная роль = кросс-агентный/машинный хендофф dump→resume, некурируемый = `infer=false`) и «Замер объёма» (вся память Claude на машине за ~2 мес = 57 файлов / 141 KB).

## Why
Пользователь уточнил: память «никакая», не курируемая — возникла как самый дешёвый перенос «сессии» между агентами/машинами (скопировал паттерн memory+session со стороны). Практика: `session` агенты не пишут вообще, `memory` — только по команде. Это переопределяет ценность дистилляции (сжатие хендоффа, не recall) и поднимает вопрос session-vs-memory. Также измерили реальный объём для оценки «нужны ли векторы/реранкеры» (нет, на годы).

## Args
- Тело `memory` (v12): + «Фактическое назначение (2026-06-03)»: транспортный буфер, `infer=false`, узкое место = нет автозаписи (отсюда вопрос MCP vs hooks); сначала автозахват → объёмы → потом курация/дистилляция; «курируемость» ≠ ручная чистка. + «Замер объёма»: 57 файлов / 141 KB за ~2 мес, рост <1 MB/год в режиме фактов.
- Вне PetBox (личная вики пользователя `D:\my\notes\notebook\wiki\ai\`): созданы заметки `эмбеддинги и реранкинг.md` и `память и сессии у агентов.md` (образовательные, по запросу пользователя).

## Outcome
Успех, борд `ideas` v12. Ключевой сдвиг: приоритет — автозахват через hooks (идея `agent-delivery`), а не ум стора; реальные объёмы/паттерны соберутся только после автозаписи, тогда и решать курацию/дистилляцию/границу session-vs-memory. Идею-вопрос `session-vs-memory` отдельным узлом НЕ заводили (пользователь предпочёл сначала разобраться заметкой в вики).

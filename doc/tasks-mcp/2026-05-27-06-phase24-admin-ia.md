---
timestamp: 2026-05-27T23:55:00+03:00
agent: claude-code
model: claude-opus-4-7
session: phase24-admin-ia
target: plan
action: update
target_file: doc/plan.md
---

## What
Добавил Phase 24 (Admin/Account IA cleanup) в `doc/plan.md` после раздела 23.7 (Follow-ups). Структура — 4 sub-phase, все помечены `[DONE]`:

- **24.1 URL унификация** — все admin URL переехали под `/ui/admin/*`. Workspace admin: `/ui/{ws}/admin/*` → `/ui/admin/ws/{ws}/*`. Sysadmin: `/ui/sys/*` → `/ui/admin/sys/*`. Account (`/ui/me/*`) сохраняет старую URL — это не admin-зона.
- **24.2 `_AccountLayout` + `_AccountSidebar`** — отдельный layout для self-service зоны (Profile / Security / Preferences). Без warning-tinted топбара, свой sidebar.
- **24.3 Плоский `_AdminSidebar` + контекстный блок проекта** — отвергнуто tree-варинат после discovery; победил плоский с context-блоком только когда вы в проекте. Лейбл "System" переименован в "Server administration" (не путать с `$system` workspace).
- **24.4 E2E** — 7 файлов обновлены, 29/29 зелёные.

В каждом sub-phase checkboxes реальные `[x]` с описаниями того, что реально сделано (имена файлов, конкретные URL, конкретные testid'ы).

## Why
Пользователь поднял четыре проблемы после Phase 23:
1. Health-poller LinqToDB error (issue 1 — отдельный коммит)
2. `/ui/sys/workspaces/test` → Login redirect (issue 2)
3. `/ui/$system` workspace switcher пустой (issue 3)
4. `/ui/$system/admin` должен вести на overview (issue 4)

Issues 1-4 закоммичены отдельно как `fix(sys): sysadmin sees + switches all workspaces; admin overview page`. Phase 24 — следующая итерация по фидбеку: непонятные лейблы сайдбара ("Workspace" / "System" путались с `$system` workspace), нет admin/ префикса в URL для всех админ-страниц.

Discovery-итерация выявила:
- Дерево как структура было отвергнуто (memory `feedback_explicit_over_implicit`: smart-поведения избегаем; collapse-state не персистит, сервисы ещё не nav-таргеты)
- Account вынесли из admin-зоны и dedicated layout — единый "admin = warning-tinted, focused" pattern для admin областей, отдельный neutral layout для personal settings

## Args
Sections edited in `doc/plan.md`:
- Добавлен новый раздел "## Phase 24: Admin/Account IA cleanup [DONE]" после 23.7 (line ~849+)
- 4 sub-phase: 24.1, 24.2, 24.3, 24.4 — все `[DONE]`, все checkboxes `[x]`
- Explicit rejection of tree-sidebar в 24.3 с обоснованием

Files affected by Phase 24 (для повторяемости):
- Routes.cs (AdminPrefix const + все админ-routes)
- 11 cshtml файлов с @page директивами
- _AdminLayout.cshtml (context label logic)
- _AdminSidebar.cshtml (полное переписывание)
- _WorkspaceAdminTabs.cshtml (Settings → Info)
- NEW: _AccountLayout.cshtml, _AccountSidebar.cshtml
- Me/Account, Me/Security, Me/Preferences (Layout swap)
- NavigationContext.cs (IsProjectRoute упрощён)
- 7 E2E файлов

## Outcome
Plan обновлён. Все 7 tasks (#18-#24) этой сессии completed. E2E suite: 29 passed / 10 skipped / 0 failed. Build чистый. Готов к финальному коммиту.

Это уже четвёртая запись в tasks-mcp для серии Phase 23/24 (после 2026-05-27-03/04/05). Series finalized.

# Бейзлайн потоков `work` и `intake` — до конвенций (2026-08-27)

Снято ДО вступления в силу `agent-routing-conventions-not-filing` (Pending) — контрольная
точка для сравнения "после". Метод: `tasks_delta(sinceVersion:0)` для полного списка ключей
+ `tasks_node_get` батчами (bodyLen:0) за `createdAt`/`updatedAt`. Разметка сошлась с числом
узлов на обеих досках (work 861/861, intake 170/170).

## work — приход/закрытие понедельно (с 2026-06-03, 84 дня)

| неделя | окно | приход | закрытие* |
|---|---|---|---|
| W0 |06-03..06-09| 62 | 51 |
| W1 |06-10..06-16| 16 | 10 |
| W2 |06-17..06-23| 4 | 13 |
| W3 |06-24..06-30| 0 | 0 |
| W4 |07-01..07-07| 206 | 191 |
| W5 |07-08..07-14| 205 | 184 |
| W6 |07-15..07-21| 86 | 69 |
| W7 |07-22..07-28| 190 | 166 |
| W8 |07-29..08-04| 14 | 9 |
| W9 |08-05..08-11| 6 | 0 |
| W10|08-12..08-18| 8 | 7 |
| W11|08-19..08-25| 43 | 25 |
| W12|08-26..09-01| 21 | 7 |

Итого 861 приход / 84 дня ≈ **10.25/день**; 738 закрытий (Done+Cancelled) / 84 дня ≈
**8.71/день**. Чистое накопление ≈ +1.5/день. Всплески W4/W5/W7 — крупные фичи, заведённые
пачками (llmrouter, deploy, search-контракт), а не органический приток находок.

*Закрытие — по `updatedAt` терминального узла, НЕ дате перехода статуса (PetBox её не
хранит отдельно). Для Done-узлов после закрытия почти нет повторных правок, так что прокси
достаточно точен, но не точен формально — возможна небольшая инфляция в недели, где узел
редактировался после Done (напр. дописан commit).

## work — сток и возраст

Статусы (861 = 702 Done + 30 Cancelled + 109 Pending + 11 Review + 8 InProgress + 1 `pending`
lowercase-опечатка): **открыто 129** (Pending 109, Review 11, InProgress 8, pending 1).

Возраст открытых (дни с `updatedAt`): медиана **30**, максимум **52**. Топ-5 самых давних:
`key-attribution`, `recall-toggles-usage-audit`, `repair-surface-v1`, `search-shadow-diff`,
`kql-pushdown-maximize-research` — все Pending, не двигались с 2026-07-05 (52 дня).

По типам: chore 427 всего / 92 открыто (21.5% открыто) — заметно дольше висит, чем feature
(295/13, 4.4%) и bug (139/24, 17.3%). Chore ведёт себя иначе, как и предполагалось.

## intake — переизмерено (для сверки метода)

170 узлов (59 открыто: reported 54, confirmed 4, triage 1; 92 done; 19 terminalcancel:
duplicate 11 + wontfix 8). Приход 170/84 ≈ **2.02/день**, закрытие 111/84 ≈ **1.32/день** —
согласуется с прежним бейзлайном (`02-snapshot-2026-08-27.md`: 1.9/день / 1.2/день на
164/104 узлах чуть более ранней выборки); расхождение — несколько узлов, заведённых/закрытых
между снятиями. Метод даёт стабильный результат.

Возраст открытых: медиана **45**, максимум **52** дня. Топ-5 самых давних не двигались с
2026-07-05: `master-key-rotation`, `audit-worktree-branch-cleanup`, `cc-headless-commit-gap`,
`deploy-config-db-shared-datadir`, `deploy-tag-race-stale-push`.

## Не выведено

- Точная дата перехода в терминальный статус — сервер её не хранит; закрытие датировано
  прокси `updatedAt` (см. выше).
- Приход/закрытие для узлов, впоследствии удалённых (18 на `work`, 18 на `intake` — точно
  совпадают по числу, возможно один и тот же сет операций) — исключены из счёта, их
  `createdAt`/`updatedAt` больше не читаемы.
- Тип-разрез для `intake` тривиален: все узлы имеют `type:issue`, различие по типам там не
  применимо.

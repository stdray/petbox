# 10 — Промт для продолжения в новой сессии

Дата составления: 2026-08-26. Скопировать текст ниже целиком в новую сессию.

---

## ПРОМТ (копировать отсюда)

Продолжаем исследование модели kind'ов PetBox. Работа большая и уже сделана наполовину —
**ничего из перечисленного не переоткрывай**, это стоило дорого.

### Где материал

- Ветка `research/kind-worlds-target` в репозитории PetBox, каталог `research/kind-worlds/`.
  В `main` не смержена и не должна быть — это черновое пространство, не документация.
- Локально: `D:\my\prj\kind-worlds-target\` (та же папка, вне репозитория).
- **Начинать с `00-INDEX.md`**, затем обязательно `01-legend.md` (словарь) и
  `01b-evolution.md` (как механизм дошёл до текущего вида).

### Что уже установлено — принимать как факт

1. **Выложен фикс** `4d956ad5` (живой сервер подтверждён): резолвинг kind'а доски переведён
   на per-board в пяти путях; три текста поверхностей исправлены. Карточка
   `$system/client-issues/custom-kind-route-undiscoverable` — Done.
2. **Открыто семь дефектов** на `$system`/`work` (см. `02-snapshot.md`). Один заблокирован
   двумя другими: уборка дубля kind `wiki` из `quartet` завозит в `$system` баги, которые
   этот дубль маскирует.
3. **Аналитика на досках:** `work/kind-worlds-model-audit` (Review) и
   `ideas/kind-worlds-decision-paths` (review, артефакт `spec_plan` положен). Обе ждут
   владельца; `Review → Done` и `review → accepted` — OWNER-ONLY, агент их не делает.
4. **Два дизайна с нуля** (`07a`, `07b`) сделаны вслепую — без чтения текущей реализации.
   Слабая форма гипотезы владельца подтверждена независимо обоими: все условия на переходе
   сводятся к одному механизму. Сильная форма («нужен язык предикатов») отклонена обоими.
5. **Четыре проверки фактами** (`09a`–`09d`) закрыли часть развилок — см. ниже.

### Что решено данными, а не вкусом

- **Где живут правила → отдельный `Blueprint`** (доска на него ссылается). Живой quartet уже
  единый кросс-kind документ; вариант «правила на доске» проигрывает.
- **Флаг enforce → вариант (c)**: условия делятся на **identity** («кто ты») и **evidence**
  («что предъявил»); принуждение выводится из семейства. Политика «identity держится
  честностью» выражается ОДИН раз на процесс, а не флагом на каждом переходе.
- **Артефакты → ничего не строим.** 0 из 7 сценариев требуют нового механизма. git-lfs и S3
  отклонены; условия возврата записаны в `09d`.
- **Класс состояния объявляется, не выводится** — на `client-issues` статус `Done` имеет
  исходящие переходы, вывод дал бы «не терминальный».
- **Закрытие процесса** — это частный случай ПЕРЕНОСА досок между процессами. Требования
  владельца: узлы остаются доступными; конфликта двух методологий во время работы нет.

### Ограничения, нарушать нельзя

- **Правило отсечения:** предложение, которое усложняет по сравнению с текущей реализацией,
  в обдумывание не берём — фиксируем фактом и идём дальше.
- **Ролей нет, есть только `owner`.** Не обобщать субъекта. Модель прав системы и модель
  ролей методологии — разные вещи (решение владельца).
- **Переименование состояний дорого:** 9 поведенческих мест, все вне документа правил;
  худшее — `AGENTS.md`, инжектируется в начало каждой сессии. Имена ПОНЯТИЙ менять дёшево,
  имена СОСТОЯНИЙ — нет.
- **Терминология:** английский термин, русский рядом. Метафор-самоделок не заводить
  (слово «мир» уже отклонено владельцем).
- Никакого рынка, конкурентов, денег, сравнений с Linear/Jira — только техническая и
  пользовательская сторона.

### Что осталось владельцу

- **Р-6:** оси тегов и словарь связей — к каталогу или к процессу.
- **Р-9:** аудит коллизий kind-слагов по девяти непросмотренным проектам. Может сделать
  ТОЛЬКО ключ владельца — сессионный ключ пускает лишь в `$system`, песочный — лишь в
  песочные проекты. Список ~45 read-only вызовов готов в `03-inventory/e-live-data.md`.
  От этого аудита зависит проверка варианта (c) и точность цены целевой модели.
- Ратификация решений, помеченных «предложено», в `05-decisions.md` (их 13).

### Что делать дальше — по убыванию ценности

1. **Не запускать новых исследований, пока владелец не ратифицировал.** Материала больше,
   чем решений; добавлять анализ бессмысленно.
2. Когда владелец даст аудит по проектам (Р-9) — проверить на нём вариант (c) и цену
   миграции. Это единственная проверка, у которой нет данных.
3. Промоутнуть предкарточки из `06-precards/PRECARDS.md` (4 штуки + 2 дополнения к живым
   карточкам), если владелец согласится. Планка промоушена там же.
4. Дефекты шага 0 — брать в работу можно в любой момент, они не зависят от целевой модели.

### Как работать

Гейты: `./build.ps1 -Target Test` И ОТДЕЛЬНО `dotnet run scripts/inspect-gate.cs` —
второй hook-only, зелёный Cake про него не доказывает ничего. Деплой: push `main`, затем
`git tag -f deploy && git push origin deploy --force`; проверять против живого
`GET https://petbox.3po.su/version` → `shortSha`, а не против `gh run watch`.

Песочница `kind-resolution-lab` (проект с флагом sandbox, ключ `sandboxOnly`, TTL до
2026-09-02) содержит живые репродукции: застрявший узел `n1`, дефект сообщения
`board_create`. Ключ в MCP-конфиге в скретчпаде прошлой сессии; если протух — выпустить
новый через `apikey_create(sandboxOnly:true)`.

---

## КОНЕЦ ПРОМТА

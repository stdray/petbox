# 01 — Легенда. Читать первым. 2026-09-10

Скил прямо предупреждает: одно слово в двух значениях отравляет все последующие стадии.
Сегодня это случилось **дважды** — оба раза факты были верны, а ось классификации нет.

## Слова, которые уже подводили

| термин | значение A | значение B | как подвело |
|---|---|---|---|
| **explore** | роль PetBox (`petbox-explore`) | встроенный агент Claude Code `Explore` | Оба имени стоят в одном абзаце прозы оркестратора. Роль не использовалась 30 дней. |
| **блокировка** | ось 1: доставка сообщения в **висящий tool-call** | ось 2: **ожидание субагента** | Замер 08.09 мерил ось 1, вывод растянули на ось 2. Неверно для Claude Code и для qwen. |
| **worktree-изоляция** | harness-managed (`isolation:"worktree"`) | instruction-driven (агент сам создаёт по AGENTS.md) | Полдня искали лекарство от болезни, которой нет: владельцу нужна вторая. |
| **фон** | qwen: `run_in_background`/дефолт по параметрам | opencode: флаг `background` за env-гейтом | Claude Code: дефолт. Три разных механизма под одним словом. |
| **бюджет/потолок** | 5450 B — тест-гейт на протокольный блок, мерится **только на claude-code** | 9400/10000 B — рантайм-лестница баннера | + третье: 10k символов, write-гейт канона. Три числа на трёх слоях, без единой политики. |
| **канон** | стор памяти `canon`, ключ `index` (инжектится ровно он) | вообще правила проекта | «Канон не влез» = выброшен целым плечом, а не подрезан. |
| **проза** | поле `notes` роли в `default-agents.json` | собранный протокольный блок | Тесты ассертят первое, гейт меряет второе. |
| **сессия** | сессия харнесса (локальный JSONL) | узел сессии в PetBox | Локальная жива, серверная пуста — разные сущности. |
| **уровень мышления** | DeepSeek: enum из 7 (`none…max`) | opencode: `variant` из реестра | droid/anthropic-путь: `thinking.budget_tokens`. `reasoning_effort` там принимается и ИГНОРИРУЕТСЯ. |
| **кошелёк** | `deepseek` — живые деньги | `opencode-go` — квота подписки | Никогда не складывать. |
| **карточка** | любой узел любой доски | карточка доски `work` | «Заведи карточку» ≠ обязательно `work`. |

## Правило стадии

Если новое наблюдение не ложится ни в одно значение из таблицы — **сначала правим легенду**,
потом пишем находку. Не наоборот.

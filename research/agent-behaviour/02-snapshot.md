# 02 — Снимок состояния на 2026-09-10

Всё ниже установлено сегодня. Источник помечен. Сверять с `origin/main`, не с локальным деревом.

## Спавн и рулёжка

- **Ось 1 (висящий tool-call):** досылка boundary-only у всех пяти харнессов. [замер 08.09, стенд `22-steering-bench.md`]
- **Ось 2 (ожидание субагента) — другая, и стенд её не трогал.** [замер 10.09]
  - Claude Code: фон по умолчанию, родитель читает ввод. [владелец + первое лицо]
  - qwen 0.23.2: `backgroundRequested = background===true || (!fork && working_dir===void 0 && name===void 0)`. [код `chunk-4F7GQGXB.js`]
  - opencode 1.18.29: фон есть, но за `OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS=true`; без флага параметр не объявлен. [код, бинарь]
  - codex 0.153.4: `spawn_agent` и `wait_agent` — РАЗНЫЕ инструменты, блокирующий дефолт бессмысленен. [код, строки бинаря, живьём НЕ проверено]
  - droid: вычеркнут владельцем.
- **qwen, замеренные ячейки:** голый спавн 37 мс фон · `working_dir` 74 618 мс foreground · `working_dir`+фон отказ валидации · `isolation:"worktree"` 510 мс фон · **голый спавн + worktree по инструкции 34 мс фон, грязное дерево родителя не мешает, `.claude/worktrees/` сохранено, уборка на агенте**. [замер 10.09]
- **Кто передаёт `working_dir`:** сам оркестратор, живой композицией. 9 вхождений в живой сессии, 0 `isolation`. НЕ в отрендеренных `agents/*.md`, НЕ в `default-agents.json`, НЕ хуком. Корень — `AGENTS.md:80-93`. [замер+код 10.09]
- `isolation:"worktree"` кладёт в `<repo>/.qwen/worktrees`, путь ЗАХАРДКОЖЕН (`chunk-KM3XVTMO.js:901-903`); `customBaseDir` в классе есть, но в этот путь не проброшен. Уборка условная: чистый — удаляется с веткой, с изменениями — сохраняется. [код 10.09]

## Проза, канон, бюджеты

- Инжектится **ровно** `store=canon,key=index`, максимум 2 записи (проект+воркспейс), не обход стора. [код `MemoryApi.cs`]
- При переполнении канон **выбрасывается целым плечом**, не подрезается. Лестница из 5 ступеней, лог в stderr и `wire.log`. [код `session-budget.ts`]
- Три числа на трёх слоях: **5450** тест-гейт протокольного блока (мерится только на `claude-code`), **9400/10000** рантайм-баннер, **10k** write-гейт канона. Единой политики нет. [код]
- Лестница деградации вшита ТОЛЬКО в `pull-memory.ts` (Claude Code). `droid-pull-memory.ts` и
  `opencode-plugin.ts` пушат без бюджета вовсе, причём **opencode — на КАЖДОМ ходе**, не только
  при старте сессии. [код 10.09, файл 10]
- **ПОПРАВКА 10.09 (стадия 3 опровергла посылку стадии 2):** размерный гейт канона накрывает
  **весь стор `canon`, любой ключ** — `EnforceCanonBudget`, `MemoryService.cs:796-798`.
  Захардкожен на `index` не гейт, а **чтение** (`MemoryApi.cs`). Асимметрия обратная той,
  что я записал: пишется много ключей и все под бюджетом, инжектится ровно один.
  Следствие: наблюдение `canon-non-index-keys-no-size-budget` может быть сформулировано
  неверно — проверить в файле 15. [код 10.09, файл 10]
- `buildProtocol` УЖЕ ветвится по харнессу для содержания; потолок по харнессу НЕ варьируется. [код + тесты]
- Набор ролей нигде не закрыт типом: `AgentRole.slug: string`, ноль union-ов в TS и C#. Добавить роль = слои `~/.petbox/agents`, кит не трогается. [код 10.09]
- Проза дублируется в ассертах: `truthfulness.test.ts` (5 мест), `default-agents-baseline.test.ts`, C# `ExpectedSlugs`. [код]

## Сессии

- Досылка транскрипта — **хук**, не MCP изнутри сессии. `Stop`/`StopFailure` → `qwen-push-session.ts` → общий `pushTranscript`. [код]
- `Stop` достигается только если очередь ввода ПУСТА: при непустой — рекурсия и возврат мимо блока хука. [код `chunk-4F7GQGXB.js:91801-91820`, ACP `acpAgent:7213-7245`]
- Следствие: **чем активнее рулишь, тем меньше пишется.** Живая сессия — 15 ходов локально, 0 на сервере.
- codex: только `SessionEnd`, ноль пушей до выхода процесса; при этом codex поддерживает событие `Stop`, оно просто НЕ подключено в `~/.codex/hooks.json`. [код]
- Протокол append-only с самолечением по 409 → поздний пуш доезжает целиком. Это дырка **долговечности**, не правильности. [код]

## Измерение

- `tools/session-usage` умеет три плеча (Claude Code, opencode, qwen). Влито `262d191a`. Тестов/линта/CI нет.
- **Три плеча — три арифметики корзин:** Claude — вход и кэш непересекаются и складываются; qwen — `cached` ВНУТРИ `prompt`, `thoughts` ВНУТРИ `candidates` (инвариант `total==prompt+candidates`, 404 хода, 0 нарушений); opencode — рассуждение ДОБАВЛЯЕТСЯ к выходу. [замер 10.09]
- База снята: `~/.petbox/baselines/baseline_20260910_145437.txt`. Сравнивать через `--days 7`, окно скользящее.

## Вне охвата
droid (вычеркнут 10.09) · модели и цены (закрыто, `wiki/deepseek-glm-qwen-zamery-2026-09-10`).

## ПОПРАВКА 2 — стадия 4 (10.09)
Роли, плечо Claude Code, 30 дней, из базы `baseline_20260910_145437.txt`:
`worker` 523 · **`worker-highstakes` 182** · `reserve` 107 · `explore` 70 · `utility` 2.
**Про `utility`:** оба вызова ДО удаления роли (20.08 и 28.08 против коммита 29.08).
Окно 30 дней захватывает доудалённый период. Аномалии нет. [файл 16]
Прежняя запись «worker-highstakes: ноль трафика» верна ТОЛЬКО для плеча opencode;
как утверждение о роли — неверна. `petbox-utility` удалена 29.08 и всё ещё вызывается.

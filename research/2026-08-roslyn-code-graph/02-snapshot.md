# Снимок состояния на 2026-08-20 — НЕ переоткрывать

Источники: wiki `graf-protiv-grep` (полевой отчёт по 12 задачам), wiki `indeksy-koda-dlya-agentov`,
`C:\Users\stdray\tools\NDepend\README.md`, комментарии `our-code` и `petproject` к wiki-странице.

## Реальный спрос (функциональное ТЗ, 8 форм)

D1. Кто использует поле / тип / метод — прямо и транзитивно, с глубиной пути.
D2. Всё, что несёт атрибут X — ОТДЕЛЬНО на типах, ОТДЕЛЬНО на методах.
D3. У каких типов есть поле типа X (напр. `ILogger`).
D4. Кто реализует интерфейс / наследует базовый тип, включая generic-обёртки (`GatedHostedService<T>`).
D5. «Делает A, но не делает B» в пределах call-графа.
D6. Связность типа: кто зависит от меня / от кого завишу я.
D7. Перечислить поверхности: MCP-инструменты, Razor `OnPost*`, фоновые сервисы.
D8. Доказать, что чего-то НЕТ во всём коде.

## Три провала NDepend — цель для своего инструмента (проверены лично)

1. `const` инлайнится → `PetBoxClaims` показан как 0 использований при **45 упоминаниях в 24 файлах**.
2. Член enum в `switch` → сравнение с `int`; `Scope.Membership` = 0 при `case Scope.Membership:` строкой ниже.
3. Атрибут на ТИПЕ невидим запросу по методам → «96 инструментов без декларации прав» = артефакт;
   реально 85 типов-носителей.

Плюс структурные пробелы: строки (claim'ы, роуты, имена Razor-партиалов), DI и фильтры пайплайна,
тестовые сборки вне модели, рефлексия.

## Числа NDepend для сравнения

- анализ всего решения ~**9 с** (`nd-analyze.ps1` ~10 с включая сборку); запрос **1–6 мс**.
- модель: 16 сборок, 1810 типов, только `src/**`.
- точность выдачи: `ClaimsPrincipal` → 68 выдал / 2 релевантно (хуже 47 grep-совпадений).

## Наш код

`PetBox.slnx` (XML-формат), 23 проекта (13 src + 3 clients-net + 6 tests + прочее), 1125 файлов,
180 510 строк. SDK 10.0.111 (`global.json` пинит 10.0.103 rollForward latestPatch).
Есть SQLite + linq2db. Есть свой KQL: `PetBox.Log.Core/Query/KqlTransformer` (1221 строка, 113 методов).

## Дефекты эксплуатации NDepend, которые свой инструмент обязан НЕ повторить

- Модель строится только из основного чекаута → агент в worktree не видит свою правку.
- Параллельные пересборки общего каталога → ложно красный гейт, заблокированный пуш (воспроизведено 3×).
- `tests/**` вне модели → «вызывающий ровно один» ложно как список; расплата на слиянии веток.

## Прежняя позиция (июль 2026) — ВНИМАНИЕ, потенциальное противоречие

Комментарий `our-code` к wiki утверждал: индекс не окупается, потому что «негде держать базу тёплой»
(5 эфемерных деревьев, нет резидентного процесса), и предлагал **урожай с `bin/`**: IL +
sequence points из portable PDB.

**Это ровно тот источник, который дал три провала выше.** IL остаётся IL, кто бы его ни читал.
Тезис «Razor покрыт, потому что компилируется в сборку» — верен; тезис «этого достаточно» — опровергнут
полевым отчётом. Консолидатор обязан явно закрыть это противоречие, а не сгладить.

## Решения владельца, действующие на входе

- Не строить полный code property graph, не гнаться за паритетом по метрикам техдолга.
- Не тащить серверную графовую БД, пока не показано, что embedded не хватает.
- Не превращать в IDE-подобный сервис.
- Триал NDepend истекает ~2026-09-03 — сверку делать, пока он жив.
- Исход «не окупается» — полноценный результат, а не провал.
